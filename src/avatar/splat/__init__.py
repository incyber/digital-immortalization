"""Building a Gaussian splat of one person from what their family had.

Two intake routes, one artefact. `routes` decides which route the material
allows and records why; `build` carries that decision through a GPU job to a
splat, a cost and an honest quality report.
"""
